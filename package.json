{
  "name": "impeccable-native",
  "version": "0.3.1",
  "description": "React Native design skill for AI coding agents. Fork of impeccable for mobile-first UI work.",
  "keywords": [
    "design",
    "react-native",
    "expo",
    "mobile",
    "ux",
    "skills",
    "ai"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/marianif/impeccable-native.git"
  },
  "engines": {
    "node": ">=18"
  },
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "clean": "rm -rf dist",
    "rebuild": "node scripts/build.js --clean",
    "test": "node --test tests/cleanup-deprecated.test.mjs && node --test tests/impeccable-paths.test.mjs && node --test tests/load-context.test.mjs && node --test tests/critique-storage.test.mjs && node --test tests/design-parser.test.mjs",
    "release": "node scripts/release.mjs"
  },
  "dependencies": {
    "marked": "^16.4.2"
  },
  "devDependencies": {
    "archiver": "^7.0.1"
  }
}
